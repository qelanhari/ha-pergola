[pytest]
# `asyncio_mode = auto` lets pytest-homeassistant-custom-component's async test
# functions and fixtures (like the `hass` fixture) work without per-test
# `@pytest.mark.asyncio` decorations. Sync tests (test_solar.py and
# test_config_flow_helpers.py) are unaffected.
asyncio_mode = auto
